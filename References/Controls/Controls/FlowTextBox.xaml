<UserControl x:Class="Controls.FlowTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="306" d:DesignWidth="505" Height="75">
    
    <RichTextBox x:Name="richTextBox" Height="75">
        <RichTextBox.Template>
            <ControlTemplate>
                <Grid>
                    <Rectangle Name="rectangle" Stroke="LightGray" StrokeThickness="1"/>
                    <RichTextBox Name="textBox" Margin="1" BorderThickness="0" AcceptsTab="True" AcceptsReturn="True"/>
                </Grid>
            </ControlTemplate>
        </RichTextBox.Template>
    </RichTextBox>
    
</UserControl>
